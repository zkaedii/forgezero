# Integration & Risk Supplement

## 1. `tests/verify.test.ts` coverage of `remote.tag_at_head` and `parseLsRemoteHash`
There are no tests in `tests/verify.test.ts` that assert against the check ID `remote.tag_at_head`.

Tests asserting `parseLsRemoteHash`:
- `it('parseLsRemoteHash handles valid output', () => { ... })`
- `it('parseLsRemoteHash returns null on empty/invalid', () => { ... })`

**Fixture/Mock Setup:**
No shared mocks or complex fixtures are used for these tests. The inputs are hardcoded string literals passed directly into the function within each `it` block.

## 2. `tests/receipt.test.ts` coverage of honesty bounds
There are no tests that explicitly assert against the exact string `"does not prove remote CI completion"`. The tests that assert the shape and presence of the honesty bounds are:
- `it('honesty block contains verified and notObservable arrays', () => { ... })`
- `it('suggested release note includes version and honesty boundary', () => { ... })` (Asserts `.toContain('Honesty bound')`)
- `it('is JSON-serializable without loss', () => { ... })` (Asserts `parsed.honesty.claim === receipt.honesty.claim`)

**Fixture references:**
These tests share a single setup fixture at the top of the `describe` block:
`const receipt = buildReleaseReceipt(REPO_ROOT);`

## 3. CLI tests exercising `bin/forge0.ts ledger record`
There are **no CLI tests** in `tests/` that invoke `bin/forge0.ts ledger record` via `execSync`.

**Fixture Patterns:**
- `tests/ledger.test.ts` tests the underlying functions directly (`recordVerifyEvent`, `recordReceiptEvent`) under `describe('ledger record events — isolated', ...)` using a temporary repository fixture created via `createTempGitRepo('0.8.8')` in a `beforeEach` hook.
- `tests/release.test.ts` checks if the CLI command string is correctly generated by the planner, using a `createPackageFixture` directory setup:
  `expect(commands).toContain('forge0 ledger record --event verify --mode release --remote --ci');`

## 4. Codebase imports of `parseLsRemoteHash` outside `src/verify/`
There are no imports or usages of `parseLsRemoteHash` outside of `src/verify/`. The only importer in the entire codebase is the test file:
- `tests/verify.test.ts:2`: `import { runVerify, parseLsRemoteHash, parseAheadBehind, parseGhRuns, summarizeCiRuns } from '../src/verify/verify.js';`

## 5. Other tag SHA vs commit SHA comparisons (particularly `src/release/`)
There are no manual SHA comparisons anywhere else in the codebase, including `src/release/`. 
The only other place that evaluates tags at `HEAD` is `src/receipt/receipt.ts`, which relies on a local `git tag --points-at HEAD` execution (no SHAs are manually compared):
- `src/receipt/receipt.ts:18`: `const tagsAtHead = trust.git?.tagsAtHead ?? [];`
- `src/receipt/receipt.ts:19`: `const expectedTagAtHead = expectedTag ? tagsAtHead.includes(expectedTag) : false;`

## 6. Naming conventions for existing check IDs
**`src/verify/verify.ts` check IDs:**
- `receipt.${c.id}` (dynamically mapped from receipt)
- `verify.cli_version_match`
- `verify.lock_version_match`
- `verify.no_blocking_findings`
- `remote.branch_at_head`
- `remote.tag_at_head`
- `remote.branch_not_ahead`
- `remote.branch_not_behind`
- `remote.sync_state`
- `ci.status`

**`src/receipt/receipt.ts` check IDs:**
- `git.clean`
- `tag.at_head`
- `changelog.version`
- `hook.installed`
- `hook.gates`
- `honesty.present`

**`src/release/`:**
No verify-related checks or IDs are defined here.

## 7. Severity levels used
The following severity levels are defined conventionally across `src/verify/types.ts:22`, `src/doctor/types.ts:25`, and `src/ledger/types.ts:24`:
`'info' | 'low' | 'medium' | 'high' | 'critical'`
